# Data policy

The public showcase intentionally does not include production data snapshots or cached optimisation outputs.

The deployed/private version uses real OHLCV market data. This public version generates small deterministic demo data at runtime so recruiters can inspect the pipeline without receiving full cached artefacts.
