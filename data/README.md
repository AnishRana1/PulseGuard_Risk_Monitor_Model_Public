# Data policy

The public showcase intentionally does not include production data snapshots or cached optimisation outputs.

The deployed/private version uses real OHLCV market data. This public codebase includes representative scaffold logic and documentation so recruiters can inspect the pipeline structure without receiving production data snapshots, cached model artefacts or the full private implementation.
